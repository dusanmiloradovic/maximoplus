(ns maximoplus.listener)
(defn start-nrepl-server
  [])
;;(ns maximoplus.listener
;;  (:require [clojure.tools.nrepl.server :as nrepl]
;;          [cider.nrepl :refer (cider-nrepl-handler)]
;;          [clojure.tools.logging :as log])
;;)
;;
;;(defn  start-nrepl-server []
;;  (when-let [nrepl-port (System/getProperty "nreplPort")]
;;      (log/info "starting the nrepl server on port " nrepl-port)
;;      (nrepl/start-server :port (Integer/parseInt nrepl-port)  :handler cider-nrepl-handler)))



